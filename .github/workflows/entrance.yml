name: entrance

on: push

jobs:
  is_dev_dependency_changed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          docker pull nginx
          docker image ls
          docker run --name nginx -p 80:80 -d nginx
          docker exec -u root nginx mkdir -p ~/keys
          docker exec -u root nginx openssl req -newkey rsa:2048 -new -nodes -x509 -days 3650 -keyout ~/keys/private.pem -out cert.pem -pubkey -batch
          docker exec -u root nginx openssl rsa -in ~/keys/private.pem -pubout > ~/keys/public.pem
          docker exec -u root nginx mkdir -p ~/keys/public
          docker exec -u root nginx cp ~/keys/public.pem ~/keys/public/public.pem

  # test2:
  #   needs: is_dev_dependency_changed
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: echo done
  #   outputs:
  #     dev_changed: ${{ needs.is_dev_dependency_changed.outputs.file_changed }}

  # test3:
  #   needs: test2
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: |
  #         if ${{ needs.test2.outputs.dev_changed }};then export FLASK_IMAGE=development-env-test; else export FLASK_IMAGE=development-env; fi





  # workflow-a:
  #   uses: ./.github/workflows/workflow-a.yml
  #   secrets: inherit

  # integration:
  #   needs: [workflow-a]
  #   # if: needs.workflow-a.result == 'success'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: echo "${{ toJSON(needs.workflow-a.conclusion) }}"
