name: a

on: push
  # workflow_call
  # push:
  #   - paths: 'a/*'

jobs:
  build:
    name: build backend go image
    runs-on: ubuntu-latest
    steps:
      - run: echo 'a running'
      - name: Sleep for 10s
        uses: juliangruber/sleep-action@v1
        with:
          time: 10s