name: Frontend e2e test

on:
  push:

jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout alpha code
        uses: actions/checkout@v3

      - uses: Charlotte-z/workflows/dir_count@main
        with:
          path: .
        id: test

      - run: echo ${{ steps.test.outputs.dir_matrix }}