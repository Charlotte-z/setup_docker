name: Manual Release

on:
  push:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Test
        uses: Charlotte-z/workflows/increase_version@main
      
      - name: Check
        run: |
          cat package.json
