name: run-integration

on: workflow_call

jobs:
  build:
    needs: [build/a, build/b]

  integration:
    name: run integration
    runs-on: ubuntu-latest
    steps:
      - run: echo "run after a, b, and integration"